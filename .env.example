# Semua opsional — aplikasi langsung jalan tanpa file .env.
PORT=3000
# Bila kosong, rahasia acak dibuat otomatis di data/.jwt_secret
JWT_SECRET=
# Lokasi folder data JSON (default: ./data)
# DATA_DIR=./data
